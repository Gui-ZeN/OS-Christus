node_modules/
build/
dist/
coverage/
test-results/
playwright-report/
.DS_Store
*.log
.env*
!.env.example
.secrets/
*.service-account.json

# Scripts/credenciais temporárias (nunca commitar)
tmp/

# Backups de dados de produção (contêm PII — nunca commitar)
_*_backups/
.rain-watch-state.json

# Configuração local do Claude Code (launch.json de preview, etc.)
.claude/
prints/
coverage-rotas/

# Relatorios de teste de mutacao (Stryker) - gerados, nao versionados
reports/
.stryker-tmp/
graphify-out/
